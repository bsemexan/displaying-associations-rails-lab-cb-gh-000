<h1>Songs</h1>
<ul>
  <% @songs.each do |artist| %>
    <li>
      <%= link_to songs.title, artist_path(artist) %>
      (<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>)
    </li>
  <% end %>
</ul>
